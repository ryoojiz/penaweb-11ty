const gridItems = document.querySelectorAll('.postsContent');

// gridItems.forEach(item => {
//   const button = item.querySelector('.postsBtn'); // Find button inside this item

//   item.addEventListener('mouseover', () => {
//     button.classList.add('postsBtn');  // Only affect this item's button
//   });

//   item.addEventListener('mouseout', () => {
//     button.classList.remove('postsBtn');
//   });
// });